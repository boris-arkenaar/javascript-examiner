\ProvidesPackage{thesis-config}

\usepackage[utf8]{inputenc}
\usepackage[colorlinks]{hyperref}
\usepackage{graphicx}
% Suppress automatic links for main glossary
\usepackage[toc, nohypertypes={main}]{glossaries}
\usepackage[square]{natbib}
\usepackage[nottoc]{tocbibind}

% Removes automatic dot at end of each glossary description
\renewcommand*{\glspostdescription}{}

% reset entries at the start of each chapter
\preto\chapter{\glsresetall}

\makeglossaries

% Modify the way `\gls`, `\glspl` (and their case-changing versions)
% and `\glsdisp` appear:

\renewcommand{\glsentryfmt}{%
  \ifglsused{\glslabel}
    {\glsgenentryfmt}% entry has been used.
    {\glshyperlink[\glsgenentryfmt]{\glslabel}}% entry hasn't been used
}

